# handover-generator

공문서 파일명을 분석하여 업무 인수인계서를 자동 생성합니다.

파일 내용은 열지 않으며, **파일명만으로** 업무를 분류하고 인수인계서를 작성합니다.

## 필수 환경

이 스킬은 **Node.js 18 이상**이 필요합니다. 대량의 JSON 데이터 처리를 위해 Node.js 스크립트를 사용합니다.

### Node.js 설치

#### Windows
```bash
# Chocolatey 사용
choco install nodejs-lts

# 또는 공식 사이트에서 다운로드
# https://nodejs.org/
```

#### macOS
```bash
# Homebrew 사용
brew install node

# 또는 공식 사이트에서 다운로드
# https://nodejs.org/
```

#### Linux
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nodejs npm

# Fedora
sudo dnf install nodejs
```

설치 후 버전 확인:
```bash
node --version  # v18.0.0 이상이어야 함
```

## 사용법

Claude Code에서 다음과 같이 말하면 됩니다:

- `"인수인계서 작성해줘"` — 공문 폴더를 분석하여 마크다운 인수인계서 생성
- `"인수인계서를 docx로 만들어줘"` — 마크다운 생성 후 Word 문서로 추가 변환
- `"공문 분석해줘"` — 공문 분류 결과만 확인
- `"인수인계서 검증해줘"` — 기존 인수인계서와 공문 목록 교차 검증

## 출력 형식

| 형식 | 설명 |
|------|------|
| **마크다운** (.md) | 기본 출력. 노션 Import 호환 |
| **Word** (.docx) | 선택 옵션. pandoc 필요 (미설치 시 자동 설치) |

## 인수인계서 구조

생성되는 인수인계서는 다음 7부 + 부록으로 구성됩니다:

| 섹션 | 내용 |
|------|------|
| **1부. 개요** | 기관 정보, 업무 영역 요약표, 문서 작성 기준 |
| **2부. 업무영역 상세** | 카테고리별 업무 설명, 세부 절차, 관련 공문, 작성자 메모란 |
| **3부. 연간 업무 달력** | 월별(3월~2월) 주요 업무 배치 |
| **4부. 위원회 및 협의체** | 위원회 목록, 법적 근거, 구성/운영 절차 |
| **5부. 대외 발송 문서** | 가정통신문 목록, 기타 대외 문서 |
| **6부. 예산 관련 사항** | 예산 항목, 지출 절차 |
| **7부. 후임자를 위한 유의사항** | 업무 우선순위, 반복 패턴, 주의사항 |
| **부록. 공문 색인** | 전체 공문 번호순 목록 + 업무 영역 매핑 |

각 카테고리에는 `[작성자 메모]` 섹션이 포함되어, 후임자가 실무 경험을 직접 보충할 수 있습니다.
